import './bootstrap';
import * as bootstrap from 'bootstrap';

// Tornar o Bootstrap disponível globalmente
window.bootstrap = bootstrap;

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
